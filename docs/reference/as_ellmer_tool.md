# Convert a deep tool to an ellmer tool

Convert a deep tool to an ellmer tool

## Usage

``` r
as_ellmer_tool(tool)
```

## Arguments

- tool:

  A `deep_tool`.

## Value

An `ellmer` tool definition.
