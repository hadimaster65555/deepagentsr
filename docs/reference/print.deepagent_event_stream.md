# Print an agent event stream

Print an agent event stream

## Usage

``` r
# S3 method for class 'deepagent_event_stream'
print(x, ...)
```

## Arguments

- x:

  A stream object returned by `agent$stream()`.

- ...:

  Unused.

## Value

Invisibly returns `x`.
