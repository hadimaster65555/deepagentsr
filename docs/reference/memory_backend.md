# Create an in-memory virtual filesystem backend

Create an in-memory virtual filesystem backend

## Usage

``` r
memory_backend(initial = list())
```

## Arguments

- initial:

  Optional named list mapping virtual paths to contents.

## Value

A backend object.
