# Create a scripted assistant tool call

Create a scripted assistant tool call

## Usage

``` r
assistant_tool_call(name, arguments = list())
```

## Arguments

- name:

  Tool name.

- arguments:

  Tool arguments.

## Value

A fake-chat script record.
